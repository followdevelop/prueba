let emails = ["persona1@gmail.com", "add123@hotmail.com", "mujer@outlook.com", "a@d.com"];


emails

const menor = emails.filter(email => email.length < 9);
menor

function esSuficientementeGrande(elemento) {
    return elemento.length >= 10;}

let mostrar = emails.map(function (esSuficientementeGrande){
    return emails;
})